{
  "name": "codetype-lambdas",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "sync-shared": "rm -rf src/shared && mkdir -p src/shared && cp ../shared/src/*.ts src/shared/"
  },
  "dependencies": {
    "@aws-sdk/client-apigatewaymanagementapi": "^3.658.0",
    "@aws-sdk/client-dynamodb": "^3.658.0",
    "@aws-sdk/lib-dynamodb": "^3.658.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/bun": "latest",
    "@types/uuid": "^10.0.0",
    "typescript": "^5.6.0"
  }
}
