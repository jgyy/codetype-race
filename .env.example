# libSQL — local file in dev, Turso URL in prod
DATABASE_URL=file:./data/codetype.db
DATABASE_AUTH_TOKEN=

# 32+ random bytes; rotate to invalidate every session cookie at once
SESSION_SECRET=replace-me-with-openssl-rand-hex-32

# Anthropic Claude — used by /api/hint
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6
